// notFound.middleware.ts — now re-exported from requestLogger.middleware.ts
// This file kept for backwards compatibility with any imports that still reference it.
export { notFoundHandler } from './requestLogger.middleware';
